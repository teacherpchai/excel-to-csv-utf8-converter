# โปรแกรมแปลงไฟล์ .xls เป็น CSV UTF-8

โปรแกรมสำหรับแปลงไฟล์ Microsoft Excel (.xls, .xlsx) เป็น CSV UTF-8 (Comma delimited) format

## ✨ คุณสมบัติ

- 🔄 แปลงไฟล์ .xls และ .xlsx เป็น CSV UTF-8
- 🌐 **เว็บแอพพลิเคชั่น** - ใช้งานผ่านเบราว์เซอร์
- 💻 **Command Line Tool** - ใช้งานผ่าน terminal
- 🇹🇭 รองรับภาษาไทย (UTF-8 with BOM)
- 📊 เก็บหัวคอลัมน์ไว้ครบถ้วน
- 📦 รองรับการแปลงหลายไฟล์พร้อมกัน

## 📦 การติดตั้ง

ติดตั้ง dependencies ที่จำเป็น:

```bash
pip install -r requirements.txt
```

## 🚀 วิธีใช้งาน

### 🌐 วิธีที่ 1: เว็บแอพพลิเคชั่น (แนะนำ)

1. รันเว็บแอป:
```bash
python app.py
```

2. เปิดเว็บเบราว์เซอร์ไปที่:
```
http://localhost:8000
```

3. อัปโหลดไฟล์ .xls หรือ .xlsx แล้วคลิกแปลงไฟล์

**คุณสมบัติเว็บแอป:**
- Drag & Drop ไฟล์
- อัปโหลดหลายไฟล์พร้อมกัน
- UI สวยงาม ใช้งานง่าย
- ดาวน์โหลดไฟล์ CSV หรือ ZIP (หลายไฟล์)

### 💻 วิธีที่ 2: Command Line Tool

#### แปลงไฟล์เดียว

```bash
python convert_xls_to_csv.py input.xls
```

จะสร้างไฟล์ `input.csv` ในโฟลเดอร์เดียวกัน

หรือระบุชื่อไฟล์ output:

```bash
python convert_xls_to_csv.py input.xls output.csv
```

#### แปลงไฟล์ทั้งโฟลเดอร์

```bash
python convert_xls_to_csv.py --batch ./input_folder
```

จะแปลงไฟล์ .xls ทั้งหมดในโฟลเดอร์ `input_folder` และเก็บผลลัพธ์ในโฟลเดอร์เดียวกัน

หรือระบุโฟลเดอร์ output:

```bash
python convert_xls_to_csv.py --batch ./input_folder ./output_folder
```

## 📋 รองรับไฟล์

- `.xls` (Excel เก่า)
- `.xlsx` (Excel ใหม่)
- HTML ที่บันทึกเป็น `.xls`
- ไฟล์ที่มีภาษาไทย

## 🎯 คุณสมบัติเพิ่มเติม

- ✅ แปลงไฟล์ .xls เป็น CSV UTF-8 (Comma delimited)
- ✅ รองรับการแปลงไฟล์เดียวหรือทั้งโฟลเดอร์
- ✅ จัดการข้อมูลตัวเลข, วันที่, และข้อความอัตโนมัติ
- ✅ สร้างไฟล์ CSV ด้วย encoding UTF-8 with BOM (Excel compatible)
- ✅ เก็บหัวคอลัมน์ไว้ครบถ้วน

## 📝 หมายเหตุ

- โปรแกรมจะอ่าน sheet แรกของไฟล์ Excel เท่านั้น
- ไฟล์ CSV ที่สร้างจะใช้ comma (,) เป็นตัวคั่น (comma delimited)
- Encoding ของไฟล์ CSV คือ UTF-8 with BOM (utf-8-sig)
- เว็บแอป: ขนาดไฟล์สูงสุด 100MB ต่อไฟล์

## 🛠️ เทคโนโลยีที่ใช้

- **Backend:** Flask (Python)
- **Frontend:** HTML5, CSS3, JavaScript
- **Libraries:** pandas, openpyxl, xlrd, lxml, html5lib

## 👨‍💻 ข้อมูลผู้พัฒนา (Developer Profile)

ระบบนี้ถูกออกแบบและพัฒนาโดยบุคลากรภายในโรงเรียน เพื่อแก้ปัญหาบริบทเฉพาะของโรงเรียนพยุห์วิทยา

- **Developer Name:** นายปองดี ไชยจันดา (Pongdee Chaichanda)
- **Position:** หัวหน้างานวัดผลและประเมินผล
- **Affiliation:** กลุ่มบริหารวิชาการ โรงเรียนพยุห์วิทยา
- **Contact:** Teacherpchai@gmail.com
- **Version:** 1.0.0 (Beta)

## 📄 License

MIT License

Copyright (c) 2025 นายปองดี ไชยจันดา (Pongdee Chaichanda)

## 🤝 Contributing

Pull requests are welcome! For major changes, please open an issue first to discuss what you would like to change.
