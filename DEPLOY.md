# คู่มือ Deploy บน Render.com

## 📋 สิ่งที่ต้องเตรียม

1. ✅ GitHub repository (มีแล้ว: https://github.com/teacherpchai/excel-to-csv-utf8-converter)
2. ✅ บัญชี Render.com (ฟรี)
3. ✅ ไฟล์ที่เตรียมไว้แล้ว:
   - `Procfile` - บอก Render ว่าต้องรันคำสั่งอะไร
   - `requirements.txt` - รวม gunicorn แล้ว
   - `render.yaml` - ไฟล์ config (optional)

## 🚀 ขั้นตอนการ Deploy

### ขั้นที่ 1: สร้างบัญชี Render.com

1. ไปที่: https://render.com
2. คลิก **"Get Started for Free"** หรือ **"Sign Up"**
3. เลือก **"Sign up with GitHub"** (แนะนำ - ง่ายที่สุด)
4. อนุญาตให้ Render เข้าถึง GitHub repository ของคุณ

### ขั้นที่ 2: สร้าง Web Service

1. หลังจาก login แล้ว คลิก **"New +"** ที่มุมบนขวา
2. เลือก **"Web Service"**

### ขั้นที่ 3: เชื่อมต่อ GitHub Repository

1. ในหน้า "Create a new Web Service":
   - **Connect account:** เลือก GitHub account ของคุณ
   - **Repository:** เลือก `excel-to-csv-utf8-converter`
   - คลิก **"Connect"**

### ขั้นที่ 4: ตั้งค่า Web Service

กรอกข้อมูลดังนี้:

- **Name:** `excel-to-csv-converter` (หรือชื่อที่คุณต้องการ)
- **Region:** เลือก `Singapore` (ใกล้ไทยที่สุด) หรือ `Oregon` (US)
- **Branch:** `main`
- **Root Directory:** (เว้นว่างไว้)
- **Runtime:** `Python 3`
- **Build Command:** 
  ```
  pip install -r requirements.txt
  ```
- **Start Command:**
  ```
  gunicorn app:app
  ```

### ขั้นที่ 5: ตั้งค่า Environment Variables (ถ้าจำเป็น)

สำหรับโปรเจคนี้ไม่จำเป็นต้องตั้งค่าเพิ่มเติม แต่ถ้าต้องการ:

- คลิก **"Advanced"** → **"Add Environment Variable"**
- สำหรับตอนนี้ไม่ต้องเพิ่มอะไร

### ขั้นที่ 6: Deploy

1. คลิก **"Create Web Service"**
2. Render จะเริ่ม build และ deploy อัตโนมัติ
3. รอประมาณ 2-5 นาที
4. เมื่อเสร็จจะได้ URL เช่น: `https://excel-to-csv-converter.onrender.com`

## ✅ ตรวจสอบการ Deploy

1. ดูที่หน้า Dashboard ของ Render
2. ถ้าเห็น **"Live"** สีเขียว = สำเร็จ!
3. คลิกที่ URL เพื่อทดสอบ

## 🔄 การอัปเดต

เมื่อคุณ push code ใหม่ไปที่ GitHub:
- Render จะ detect อัตโนมัติ
- จะ rebuild และ redeploy ใหม่เอง
- ไม่ต้องทำอะไรเพิ่ม!

## 💡 Tips

1. **Free Tier มีข้อจำกัด:**
   - Service จะ sleep ถ้าไม่มีการใช้งาน 15 นาที
   - ครั้งแรกที่เปิดอาจช้า (ต้องรอ wake up)
   - ใช้ได้ 750 ชั่วโมง/เดือน

2. **ถ้า Service Sleep:**
   - เปิด URL ครั้งแรกจะใช้เวลาประมาณ 30-60 วินาที
   - หลังจากนั้นจะเร็วขึ้น

3. **ตรวจสอบ Logs:**
   - คลิกที่ service → **"Logs"** tab
   - ดู error messages ถ้ามีปัญหา

## 🆘 Troubleshooting

### Build Failed
- ตรวจสอบ `requirements.txt` ว่าถูกต้อง
- ดู Logs เพื่อดู error message

### Service ไม่ทำงาน
- ตรวจสอบ Start Command: `gunicorn app:app`
- ตรวจสอบว่า `Procfile` มีอยู่

### ไฟล์ไม่พบ
- ตรวจสอบว่าไฟล์ทั้งหมดถูก push ไป GitHub แล้ว
- ตรวจสอบ Root Directory setting

## 📞 ต้องการความช่วยเหลือ?

- Render Documentation: https://render.com/docs
- Render Community: https://community.render.com
