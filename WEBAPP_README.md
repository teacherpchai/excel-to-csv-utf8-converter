# เว็บแอพพลิเคชั่นแปลงไฟล์ .xls เป็น CSV UTF-8

เว็บแอพพลิเคชั่นสำหรับแปลงไฟล์ Microsoft Excel (.xls, .xlsx) เป็น CSV UTF-8 (Comma delimited) format

## คุณสมบัติ

- 🌐 ใช้งานผ่านเว็บเบราว์เซอร์ ไม่ต้องติดตั้งโปรแกรม
- 📁 รองรับการอัปโหลดหลายไฟล์พร้อมกัน
- 🎨 UI สวยงาม ใช้งานง่าย
- 🇹🇭 รองรับภาษาไทย (UTF-8 with BOM)
- 📊 เก็บหัวคอลัมน์ไว้ครบถ้วน
- 📦 รองรับการดาวน์โหลดหลายไฟล์เป็น ZIP

## การติดตั้ง

1. ติดตั้ง dependencies:

```bash
pip install -r requirements.txt
```

2. รันเว็บแอปพลิเคชั่น:

```bash
python app.py
```

หรือ:

```bash
flask run
```

3. เปิดเว็บเบราว์เซอร์ไปที่:

```
http://localhost:5000
```

## การใช้งาน

1. **อัปโหลดไฟล์:**
   - คลิกที่พื้นที่อัปโหลด หรือ
   - ลากไฟล์มาวาง (Drag & Drop)

2. **ตรวจสอบไฟล์:**
   - ดูรายการไฟล์ที่เลือก
   - ลบไฟล์ที่ไม่ต้องการ

3. **แปลงไฟล์:**
   - คลิกปุ่ม "แปลงไฟล์"
   - ไฟล์ CSV จะถูกดาวน์โหลดอัตโนมัติ
   - ถ้ามีหลายไฟล์ จะรวมเป็น ZIP

## รองรับไฟล์

- `.xls` (Excel เก่า)
- `.xlsx` (Excel ใหม่)
- HTML ที่บันทึกเป็น `.xls`
- ไฟล์ที่มีภาษาไทย

## ข้อจำกัด

- ขนาดไฟล์สูงสุด: 100MB ต่อไฟล์
- อ่านเฉพาะ Sheet แรกของไฟล์ Excel
- ไฟล์ CSV ที่ได้ใช้ comma (,) เป็นตัวคั่น

## เทคโนโลยีที่ใช้

- **Backend:** Flask (Python)
- **Frontend:** HTML5, CSS3, JavaScript
- **Library:** pandas, openpyxl, xlrd, lxml

## หมายเหตุ

- ไฟล์ที่อัปโหลดจะถูกเก็บชั่วคราวและลบทันทีหลังแปลงเสร็จ
- การแปลงไฟล์จะทำบนเซิร์ฟเวอร์
- แนะนำให้ใช้ Chrome, Firefox, หรือ Safari เวอร์ชันล่าสุด
